services:
  - type: web
    name: hackathon-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.backend_api.fast_api_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11